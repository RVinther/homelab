---
apiVersion: v1
kind: PersistentVolumeClaim

metadata:
  name: emulatorjs-data
  namespace: gaming

spec:
  accessModes:
    - ReadWriteOnce

  storageClassName: longhorn

  resources:
    requests:
      storage: 100Gi
